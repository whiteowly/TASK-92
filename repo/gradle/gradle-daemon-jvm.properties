# Gradle Daemon JVM criteria (Gradle 8.8+).
# Forces the Gradle Daemon itself to run on Java 21 so plugins compiled as
# Java 17+ bytecode (e.g. Spring Boot 3.x) can be loaded. If no matching JDK
# is installed locally, Gradle will auto-provision one via toolchains.
toolchainVersion=21
